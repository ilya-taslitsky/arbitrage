package com.crypto.arbitrage.providers.orc.models;

/** Swap types (exact input or exact output). */
public enum OrcSwapType {
  EXACT_IN, // Swap with exact input amount
  EXACT_OUT // Swap with exact output amount
}
